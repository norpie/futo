# FUTO Keyboard Build Environment Variables
# Copy this file to .env and fill in your actual values

# =============================================================================
# ANDROID SIGNING CONFIGURATION
# =============================================================================

# Keystore password (used to access the keystore file)
ORG_GRADLE_PROJECT_keystorePassword=your_keystore_password_here

# Key alias (the name of your signing key within the keystore)
ORG_GRADLE_PROJECT_keyAlias=your_key_alias_here

# Key password (password for the specific private key)
ORG_GRADLE_PROJECT_keyPassword=your_key_password_here

# =============================================================================
# OPTIONAL: Custom Keystore Path
# =============================================================================

# If you want to use a different keystore location (optional)
# ORG_GRADLE_PROJECT_keystorePath=/path/to/your/custom.keystore

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================

# To create a keystore for signing:
# keytool -genkey -v -keystore release.keystore \
#   -alias futo-keyboard \
#   -keyalg RSA \
#   -keysize 2048 \
#   -validity 10000 \
#   -storepass YOUR_STORE_PASSWORD \
#   -keypass YOUR_KEY_PASSWORD \
#   -dname "CN=Your Name, OU=Development, O=Your Organization, L=Your City, ST=Your State, C=US"

# For GitHub Actions, encode your keystore:
# base64 -w 0 release.keystore > keystore_base64.txt
# Then add as ANDROID_KEYSTORE secret in GitHub

# For local development, copy this file to .env and fill in actual values